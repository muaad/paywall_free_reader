<!DOCTYPE html>
<html>
  <head>
    <title>PaywallFreeReader</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="py-3 mb-3 border-bottom">
      <div class="container d-grid gap-3 align-items-center" style="grid-template-columns: 1fr 2fr;">
        <div>
          <h3>Paywall-Free Reader</h3>
        </div>

        <div class="d-flex align-items-center">
          <form class="w-100 me-3" action="/urls/read" method="GET">
            <input type="search" value="<%= params[:url]%>" class="form-control" placeholder="Enter URL here..." name="url"aria-label="Search">
          </form>

          <div class="flex-shrink-0 dropdown">
            
          </div>
        </div>
      </div>
    </header>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
